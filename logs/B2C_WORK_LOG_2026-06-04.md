# B2C Work Log - 2026-06-04

## User Direction

Requests:
- Create a B2C block for a retail store.
- Do not mix blocks together.
- Do not rework previous marketplace or B2B blocks.
- B2C retail must be separate.
- Use a separate folder, separate log, and separate GitHub direction.
- Log actions, requests, responses, code, and checks.

## Actions

- Stopped using `marketplace-crm` as the B2C implementation target.
- Restored marketplace-facing files so the existing project is not converted into B2C.
- Created a separate folder:
  - `D:\Codex\CRM\retail-crm`
- Created separate B2C app files:
  - `index.html`
  - `styles.css`
  - `app.js`
  - `README.md`
  - `.gitignore`
- Created this separate B2C log:
  - `logs/B2C_WORK_LOG_2026-06-04.md`

## Code Scope

The new B2C app includes:
- Dashboard for retail store KPIs.
- New retail receipt form.
- Receipt journal.
- Store stock table.
- Cash/POS section.
- Local demo state in browser `localStorage`.

The app is intentionally standalone and has no dependency on `marketplace-crm`.

## GitHub Scope

This folder is intended to become a separate Git/GitHub repository.

Local Git actions:
- Ran `git init` inside `D:\Codex\CRM\retail-crm`.
- `git status --short` initially failed without safe-directory because Windows reported sandbox/user ownership mismatch.
- Used inline `git -c safe.directory=D:/Codex/CRM/retail-crm ...` instead of changing global git config.
- Ran `git add .`.
- Git returned Windows LF/CRLF warnings only.
- Ran `git commit -m "Initial B2C retail CRM"`.
- Commit response:
  - `[main (root-commit) 701f8eb] Initial B2C retail CRM`
  - `6 files changed, 697 insertions(+)`

Pending external step:
- Create or connect a GitHub remote repository after the repository name, owner/account, and visibility are confirmed.
- Current local repo has no GitHub remote configured yet.
- Checked GitHub CLI with `gh --version`.
- Response: `gh` is not installed or not available in PATH, so a GitHub remote repository cannot be created from this environment right now.

## 11:35 - Next step question

User request:
- `що далі ?`

Response plan:
- Keep B2C as a separate project in `D:\Codex\CRM\retail-crm`.
- Next practical steps are:
  - connect a separate GitHub remote;
  - run the B2C app locally for review;
  - define the retail store document flow;
  - then expand modules one by one: checkout, receipts, stock, cash/POS, returns, reports, integrations.

## 12:50 - GitHub remote provided

User provided:
- GitHub repository page screenshot for `1973zahar/retail-crm`.
- Remote URL visible on GitHub:
  - `https://github.com/1973zahar/retail-crm.git`

Actions:
- Added GitHub remote:
  - `git remote add origin https://github.com/1973zahar/retail-crm.git`
- Verified remote:
  - `origin https://github.com/1973zahar/retail-crm.git (fetch)`
  - `origin https://github.com/1973zahar/retail-crm.git (push)`
- Next action:
  - commit this log entry;
  - push `main` to GitHub.

## 12:50 - Push attempts

Actions and responses:
- Committed GitHub remote log:
  - `56756fd Add GitHub remote to B2C log`
- Ran `git push -u origin main`.
  - Response: `fatal: unable to access 'https://github.com/1973zahar/retail-crm.git/': schannel: AcquireCredentialsHandle failed: SEC_E_NO_CREDENTIALS`.
- Checked Git Credential Manager:
  - `git credential-manager --version`
  - Response: `2.7.3+5fa7116896c82164996a609accd1c5ad90fe730a`.
- Tried browser login:
  - `git credential-manager github login --url https://github.com --username 1973zahar --browser --force`
  - Response: command completed, but push still had `SEC_E_NO_CREDENTIALS`.
- Tried device login:
  - `git credential-manager github login --url https://github.com --username 1973zahar --device --force`
  - Response: timed out without returning a device code in this shell.
- Ran `git credential-manager diagnose`.
  - Response summary: credential storage OK, but GitHub API/auth diagnostics failed; diagnostic file was generated.
  - Removed `gcm-diagnose.log` because Git Credential Manager warns it may include sensitive information.
- Set local repo TLS backend:
  - `git config http.sslBackend openssl`
  - Response: `openssl`.
- Verified remote reachability:
  - `git ls-remote origin`
  - Response: success with no refs, expected for an empty GitHub repo.
- Retried push with verbose:
  - `git push -u origin main --verbose`
  - Response: `Pushing to https://github.com/1973zahar/retail-crm.git`, then Git exited with code `1`.

Current blocker:
- The GitHub repository exists and the local `origin` remote is set.
- Network access to the remote works.
- Push is blocked by missing CLI credentials/auth prompt in this environment.

## 12:50 - User asks how to authorize Git Credential Manager

User request:
- `як`
- Selected text: `авторизувати Git Credential Manager`

Response:
- Provide exact PowerShell steps for Git Credential Manager browser login, device login fallback, and final `git push -u origin main`.

## 13:35 - User completed GitHub authorization and push

User provided:
- Screenshot from Administrator Windows PowerShell.

Observed terminal response:
- User ran:
  - `cd D:\Codex\CRM\retail-crm`
  - `git config --global --add safe.directory D:/Codex/CRM/retail-crm`
  - `git credential-manager github login --url https://github.com --username 1973zahar --browser --force`
  - `git push -u origin main`
- Git Credential Manager response:
  - `info: please complete authentication in your browser...`
- Push response:
  - `Enumerating objects: 33, done.`
  - `Counting objects: 100% (33/33), done.`
  - `Writing objects: 100% (33/33), 10.02 KiB, done.`
  - `To https://github.com/1973zahar/retail-crm.git`
  - `* [new branch] main -> main`
  - `branch 'main' set up to track 'origin/main'.`

Status:
- Separate B2C repository was pushed to GitHub successfully.

## 13:46 - Prototype build started

User request:
- `будуємо прототип !`

Planned prototype scope:
- Keep work inside the separate B2C folder `D:\Codex\CRM\retail-crm`.
- Build a usable retail-store prototype, not a landing page.
- Add operational B2C flows:
  - dashboard KPIs;
  - checkout with multi-line cart;
  - receipt journal;
  - returns from receipts;
  - stock receiving and stock table;
  - cash/POS shift controls;
  - daily report;
  - internal operation log.
- Do not touch `marketplace-crm`.

Implementation:
- Replaced the minimal `app.js` with B2C prototype build:
  - `APP_VERSION = "2026.06.04.2"`
  - `APP_BUILD = "20260604-b2c-prototype-1"`
- Added state model for:
  - checkout draft;
  - products;
  - customers;
  - stock;
  - receipts;
  - returns;
  - cash shifts;
  - stock receipts;
  - operation audit log.
- Added B2C screens:
  - `Панель`;
  - `Новий чек`;
  - `Чеки`;
  - `Повернення`;
  - `Залишки`;
  - `Каса/POS`;
  - `Звіт дня`;
  - `Журнал`.
- Added actions:
  - add product to cart;
  - edit cart qty and discount;
  - post receipt;
  - create full return from receipt;
  - receive stock into store;
  - open cash shift;
  - close cash shift;
  - reset demo data.
- Updated `styles.css` for:
  - product picker;
  - cart table;
  - KPI cards;
  - shift summary;
  - operation log.
- Updated `index.html` cache-busters to:
  - `20260604-b2c-prototype-1`.
- Updated `README.md` with the current prototype scope.

Verification:
- Ran:
  - `node --check app.js`
- Response:
  - success, no syntax errors.
- Found and fixed one runtime risk:
  - `normalizeReceipt()` previously depended on global `state` during initial state load.
  - changed it to receive `products` directly from `normalizeState()`.
- Ran Node smoke-test with minimal `document` and `localStorage` stubs.
- Response:
  - `render-ok appHtml=4102 navHtml=539`.

## 13:46 - Prototype push confirmed

User response:
- `є`

Verification:
- Ran `git fetch origin`.
  - Response: success.
- Ran `git ls-remote origin refs/heads/main`.
  - Response: `c0188954e4c88fbf30873b575fb7f10f8fa7793d refs/heads/main`.

Status:
- GitHub `main` contains prototype commit `c018895 Build B2C retail prototype`.

## 14:54 - User reports git-remote-https crash

User provided:
- Screenshot of Windows PowerShell and application error dialog.

Observed error:
- `git-remote-https.exe - Ошибка приложения`
- Memory read error:
  - `Инструкция по адресу ... обратилась к памяти по адресу 0x0000000000000000. Память не может быть read.`

Local checks:
- Ran `git status -sb`.
  - Response: `## main...origin/main [ahead 1]`.
- Ran `git log --oneline --decorate -5`.
  - Response confirms:
    - local `HEAD` is `50b582f Log B2C prototype push confirmation`;
    - `origin/main` is `c018895 Build B2C retail prototype`.
- Ran `git config --get http.sslBackend`.
  - Response: `openssl`.

Diagnosis:
- The B2C prototype commit is already on GitHub.
- Only the latest local log commit remains unpushed.
- The error is a Git for Windows HTTPS transport crash, not an app-code problem.

## 14:59 - SSH key created, ssh-agent failed

User provided:
- Screenshot from Administrator Windows PowerShell.

Observed terminal response:
- `git push` over HTTPS succeeded for prototype commit:
  - `523e50f..c018895 main -> main`.
- User generated SSH key:
  - `ssh-keygen -t ed25519 -C "1973zahar@github" -f "$env:USERPROFILE\.ssh\id_ed25519_github"`.
- SSH key files created:
  - `C:\Users\User\.ssh\id_ed25519_github`
  - `C:\Users\User\.ssh\id_ed25519_github.pub`
- `Start-Service ssh-agent` failed:
  - `Service 'OpenSSH Authentication Agent (ssh-agent)' cannot be started`.
- `ssh-add` failed:
  - `Error connecting to agent: No such file or directory`.
- Public key was copied:
  - `Get-Content "$env:USERPROFILE\.ssh\id_ed25519_github.pub" | Set-Clipboard`.

Response:
- Explain that `ssh-agent` is not required if the key has no passphrase.
- Use an SSH config entry or Git `core.sshCommand` so Git uses the named key directly.

## 15:03 - User asks where to add SSH key

User request:
- `де ключ вносити ?`

Context:
- User is on GitHub repository settings page.
- The left menu shows repository-level `Deploy keys`.

Response:
- For normal Git push, add the public key in account settings:
  - profile avatar -> Settings -> SSH and GPG keys -> New SSH key.
- Repository-level `Deploy keys` can also work only if `Allow write access` is enabled, but account-level SSH key is the cleaner setup for this workflow.

## 15:13 - User asks where the SSH key is

User request:
- `де ключ ?`

Context:
- User tested SSH:
  - `ssh -i "$env:USERPROFILE\.ssh\id_ed25519_github" -o IdentitiesOnly=yes -T git@github.com`
- GitHub responded:
  - `Permission denied (publickey).`

Response:
- The public key is in:
  - `C:\Users\User\.ssh\id_ed25519_github.pub`
- The private key is:
  - `C:\Users\User\.ssh\id_ed25519_github`
- Only the `.pub` public key must be pasted into GitHub.
- The private key must not be pasted or shared.

## 15:18 - SSH key added confirmation

User response:
- `є`

Checks:
- Ran `git remote -v`.
  - Response:
    - `origin git@github.com:1973zahar/retail-crm.git (fetch)`
    - `origin git@github.com:1973zahar/retail-crm.git (push)`
- Ran `git config --get core.sshCommand`.
  - Response:
    - `ssh -i C:/Users/User/.ssh/id_ed25519_github -o IdentitiesOnly=yes`
- Ran `git status -sb`.
  - Response:
    - `## main...origin/main [ahead 1]`
    - `M logs/B2C_WORK_LOG_2026-06-04.md`
- Tried SSH auth from sandbox:
  - `ssh -i "$env:USERPROFILE\.ssh\id_ed25519_github" -o IdentitiesOnly=yes -T git@github.com`
  - Response:
    - timeout;
    - `Warning: Identity file C:\Users\User\.ssh\id_ed25519_github not accessible: Permission denied.`

Status:
- Git remote is now SSH.
- The user's PowerShell should be able to push.
- This sandbox cannot read the private key, so it cannot push via SSH.

## 15:32 - User confirms SSH step

User response:
- `є`

Checks attempted:
- Ran `git fetch origin`.
  - Response: timeout; sandbox cannot access `C:\Users\User\.ssh\id_ed25519_github`.
- Ran `git ls-remote origin refs/heads/main`.
  - Response: timeout; sandbox cannot access `C:\Users\User\.ssh\id_ed25519_github`.

Status:
- Cannot verify remote from sandbox because the SSH private key belongs to the Windows user profile.
- User PowerShell is the correct place to run the final `git push`.
- This log entry is intentionally not committed immediately to avoid creating another push-only log cycle.

## 16:23 - SSH key still denied by GitHub

User provided:
- Screenshot from Administrator Windows PowerShell.

Observed terminal response:
- Public key shown:
  - `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIEtWC01tWbjUXwZKVSCJ2WkfutqEibZJw5c+WWOFSbma 1973zahar@github`
- `ssh -i "$env:USERPROFILE\.ssh\id_ed25519_github" -o IdentitiesOnly=yes -T git@github.com`
  - prompts for passphrase;
  - then returns `git@github.com: Permission denied (publickey).`
- `git push`
  - prompts for passphrase;
  - then returns `git@github.com: Permission denied (publickey).`

Diagnosis:
- The private key is readable by the user's PowerShell because SSH asks for its passphrase.
- GitHub is not accepting the matching public key.
- Most likely causes:
  - public key was not added to GitHub yet;
  - public key was added to repository deploy keys without write access;
  - public key was pasted with a typo or line break;
  - public key was added under a different GitHub account.

Response:
- Re-add the exact `.pub` key as an account-level key:
  - Avatar -> Settings -> SSH and GPG keys -> New SSH key.
- Or if using repository Deploy keys, enable write access.

## 16:23 - User asks to repeat SSH key steps from beginning

User request:
- `давай ще раз з початку де взяти ключ і куди вставити його`

Response:
- Explain how to read/copy the public key from:
  - `C:\Users\User\.ssh\id_ed25519_github.pub`
- Explain that the private key must not be shared:
  - `C:\Users\User\.ssh\id_ed25519_github`
- Explain where to paste the public key:
  - GitHub avatar -> Settings -> SSH and GPG keys -> New SSH key.
- Provide final SSH test and `git push` commands.

## 16:41 - GitHub confirms SSH key was added, SSH still asks passphrase

User provided:
- Screenshot of GitHub email:
  - `[GitHub] A new SSH authentication public key was added to your account`
  - key title: `retail-crm PC`
  - fingerprint: `SHA256:PnFM1R6kUIPtxILTm6AkMwceNI198BGN4ImVXFAudCM`
- Screenshot of PowerShell:
  - public key printed from `id_ed25519_github.pub`;
  - `ssh -i "$env:USERPROFILE\.ssh\id_ed25519_github" -o IdentitiesOnly=yes -T git@github.com`
  - SSH prompts: `Enter passphrase for key ...`
  - after attempts, GitHub returns: `Permission denied (publickey)`.

Diagnosis:
- GitHub account has the public key.
- The remaining blocker is the private key passphrase.
- If the passphrase entered in PowerShell is wrong, or Enter is pressed for a key that has a passphrase, SSH cannot use the private key and GitHub denies access.

Response:
- Enter the exact passphrase that was created during `ssh-keygen`.
- If the passphrase is forgotten, create a new key without passphrase and add the new `.pub` key to GitHub.

## 16:45 - SSH push succeeded

User provided:
- Screenshot from Administrator Windows PowerShell.

Observed terminal response:
- User created a second SSH key without passphrase:
  - `id_ed25519_github_2`
  - `id_ed25519_github_2.pub`
- User copied the new public key to clipboard.
- User configured repo SSH command:
  - `git config core.sshCommand "ssh -i C:/Users/User/.ssh/id_ed25519_github_2 -o IdentitiesOnly=yes"`
- SSH test succeeded:
  - `Hi 1973zahar! You've successfully authenticated, but GitHub does not provide shell access.`
- `git push` succeeded:
  - `To github.com:1973zahar/retail-crm.git`
  - `c018895..81f7d73 main -> main`

Status:
- SSH setup works.
- Local commits through `81f7d73 Log SSH setup status` were pushed to GitHub successfully.

## 16:48 - Continue prototype build

User request:
- `вперід`

Planned scope:
- Continue only in `D:\Codex\CRM\retail-crm`.
- Add prototype layer:
  - product catalog screen;
  - customer/loyalty screen;
  - loyalty discount in checkout;
  - receipt print/preview panel;
  - partial returns;
  - refreshed build/version.
- Keep `marketplace-crm` untouched.

Implementation:
- Updated build metadata:
  - `APP_VERSION = "2026.06.04.3"`
  - `APP_BUILD = "20260604-b2c-prototype-2"`
- Added customer loyalty model:
  - `standard`;
  - `silver` with 3% discount;
  - `gold` with 5% discount.
- Added checkout loyalty discount:
  - subtotal;
  - loyalty discount;
  - final total.
- Added receipt print/preview panel in `Чеки`.
- Added partial return form:
  - select receipt/product;
  - return quantity;
  - reason;
  - returnable quantity control.
- Added product catalog screen:
  - product table;
  - create new SKU;
  - initial stock quantity.
- Added customer screen:
  - customer table;
  - create customer card;
  - select customer for checkout.
- Updated daily report margin to use actual receipt total after loyalty discount.
- Updated `index.html` cache-busters:
  - `20260604-b2c-prototype-2`.
- Updated `README.md` with:
  - loyalty discount;
  - receipt print preview;
  - partial returns;
  - product catalog;
  - customer loyalty cards.

Verification:
- Ran `node --check app.js`.
  - Response: success, no syntax errors.
- First `rg` verification command failed because PowerShell string quoting broke on a double quote.
- Re-ran `rg` with safe quoting.
  - Response confirmed `b2c-prototype-2`, catalog/customers renderers, product/customer actions, partial return, receipt print, and loyalty constants.
- Ran Node smoke-test with minimal DOM/localStorage stubs.
  - Response: `render-ok appHtml=4423 navHtml=674`.

## 17:02 - LAN viewer request

User request:
- `є`
- `зроби лан переглядалку`

Interpretation:
- Build a separate LAN viewer for the B2C retail prototype.
- Keep work isolated inside `D:\Codex\CRM\retail-crm`.
- Do not modify marketplace or B2B blocks.

Planned implementation:
- Add `serve-lan.ps1`.
- Serve static B2C files from the current project folder.
- Bind to all network interfaces so the prototype can be opened from another device in the same LAN.
- Print local and LAN URLs.
- Update README with run instructions.

Implementation:
- Added `serve-lan.ps1`.
  - Default port: `8790`.
  - Root folder defaults to the B2C project folder.
  - Serves `/index.html`, `app.js`, `styles.css`, and other static files.
  - Blocks path traversal outside `retail-crm`.
  - Prints:
    - local URL;
    - LAN URLs for detected IPv4 addresses;
    - stop instruction.
- Updated `README.md` with LAN viewer launch command:
  - `powershell -ExecutionPolicy Bypass -File .\serve-lan.ps1 -Port 8790`

Verification:
- Ran PowerShell parser check for `serve-lan.ps1`.
  - Response: `ps-parse-ok`.
- Ran `node --check app.js`.
  - Response: success, no syntax errors.
- Started `serve-lan.ps1` on port `8790`, fetched `http://127.0.0.1:8790/index.html`, then stopped the test process.
  - Response: `lan-viewer-ok status=200 length=1383`.
  - Response: `stopped pid=8088`.

Codex launch diagnostics:
- Attempted to leave `serve-lan.ps1` running from Codex on port `8790`.
  - Initial response inside the same tool command: `server-started pid=21052 status=200 local=http://127.0.0.1:8790/index.html`.
  - Detected LAN URLs:
    - `http://100.64.4.110:8790/index.html`
    - `http://172.21.176.1:8790/index.html`
    - `http://192.168.0.161:8790/index.html`
  - Follow-up response from a separate tool command: `Invoke-WebRequest : Cannot connect to remote server`.
  - `Get-Process -Id 21052` returned no process.
- Diagnostic run with `Start-Process -RedirectStandardOutput/-RedirectStandardError` failed in this PowerShell environment.
  - Response: `Start-Process : Item has already been added. Key in dictionary: "Path" Adding key: "PATH"`.
  - This created empty diagnostic files:
    - `logs/lan-viewer-test.out.log`
    - `logs/lan-viewer-test.err.log`
- Re-ran without redirects on port `8791`.
  - Response: `two-requests-ok r1=200 r2=200 alive=True pid=26704`.
  - Response: `stopped pid=26704`.
- Tried a detached start on port `8792`.
  - Initial response inside the same tool command: `detached-start pid=34220 status=200 port=8792`.
  - Follow-up response from a separate tool command: `Invoke-WebRequest : Cannot connect to remote server`.
  - `Get-Process -Id 34220` returned no process.
- Removed empty diagnostic files created by the failed redirect test:
  - `logs/lan-viewer-test.out.log`
  - `logs/lan-viewer-test.err.log`

Conclusion:
- `serve-lan.ps1` works while running.
- Codex tool execution does not keep the hidden child process alive between tool commands.
- For live LAN viewing, run the LAN viewer from the user's own Windows PowerShell:
  - `cd D:\Codex\CRM\retail-crm`
  - `powershell -ExecutionPolicy Bypass -File .\serve-lan.ps1 -Port 8790`

## 17:16 - Product source correction

User request:
- `в роздрібі новий товар не створюємо тільки імпорт із ескюель`

Interpretation:
- In the B2C retail block, product cards/SKU must not be created manually.
- Product catalog must be imported/synchronized from SQL only.
- Keep the change isolated inside `D:\Codex\CRM\retail-crm`.

Implementation:
- Updated build metadata:
  - `APP_VERSION = "2026.06.04.4"`.
  - `APP_BUILD = "20260604-b2c-sql-import"`.
  - `STORAGE_KEY = "retail-crm-b2c-v3"` so old demo products created manually do not carry forward.
- Added SQL product source model:
  - `SQL_PRODUCT_SOURCE = "MSSQL:dbo.RetailProducts"`.
  - `sqlProductSnapshot` as the prototype SQL import source.
  - `productFromSql()` and `stockFromSql()` mapping functions.
- Changed seed products and stock to come from the SQL snapshot.
- Added `productImport` state with:
  - source;
  - row count;
  - last import timestamp;
  - mode.
- Changed catalog UI:
  - removed manual "Новий товар" form;
  - removed `create-product` action;
  - added "Імпорт товарів з SQL" panel;
  - added SQL query preview;
  - added "Синхронізувати з SQL" button.
- Replaced `createProduct()` with `syncProductsFromSql()`.
- Updated submit handler from `create-product` to `sync-sql-products`.
- Updated `README.md` to state that SKU creation is disabled and products come only from SQL import.
- Added CSS for the SQL query block.
- Removed remaining fallback/UI wording that implied "new product" creation:
  - product fallback name changed to `Товар з SQL`;
  - catalog note now says SKU is not created manually.

Verification:
- Ran `node --check app.js`.
  - Response: success, no syntax errors.
- Ran `rg` verification for SQL import flow and old manual product creation tokens.
  - Response confirmed:
    - `APP_BUILD = "20260604-b2c-sql-import"`;
    - `SQL_PRODUCT_SOURCE`;
    - `sqlProductSnapshot`;
    - `sync-sql-products`;
    - `syncProductsFromSql()`;
    - no `createProduct` handler in app code.
- First DOM smoke-test used the default dashboard view and failed because the SQL block is rendered only in the catalog view.
  - Response: `Error: catalog SQL flow not rendered as expected`.
- Re-ran DOM smoke-test with `currentView: "catalog"`.
  - Response: `render-ok appHtml=3657 navHtml=674`.
- Ran final manual-creation token check:
  - Command searched `app.js`, `index.html`, and `README.md` for `create-product`, `createProduct`, `Додати SKU`, and `Новий товар`.
  - Response: `no-manual-product-create`.

## 17:25 - Stock receiving source correction

User request:
- `Надходження в магазин також тільки через імпорт`

Interpretation:
- In B2C retail, stock receiving/oprybutkuvannia must not be entered manually.
- Store receiving documents and current store stock must come from SQL import only.
- Keep the change isolated inside `D:\Codex\CRM\retail-crm`.

Implementation:
- Updated build metadata:
  - `APP_VERSION = "2026.06.04.5"`.
  - `APP_BUILD = "20260604-b2c-sql-stock-import"`.
  - `STORAGE_KEY = "retail-crm-b2c-v4"` so old manually entered stock receiving data does not carry forward.
- Added SQL stock receiving source model:
  - `SQL_STOCK_RECEIPT_SOURCE = "MSSQL:dbo.RetailStockReceipts"`.
  - `sqlStockReceiptSnapshot` as the prototype SQL import source.
  - `stockReceiptFromSql()` mapping function.
- Added `stockImport` state with:
  - source;
  - row count;
  - last import timestamp;
  - mode.
- Seed `stockReceipts` now comes from `sqlStockReceiptSnapshot`.
- Changed stock UI:
  - removed manual "Надходження в магазин" form;
  - removed `receive-stock` action;
  - added "Імпорт надходжень з SQL" panel;
  - added SQL query preview;
  - added "Імпортувати надходження з SQL" button;
  - added latest imported receiving documents table.
- Replaced `receiveStock()` with `syncStockReceiptsFromSql()`.
- Updated submit handler from `receive-stock` to `sync-sql-stock-receipts`.
- Updated `README.md` to state that stock receiving is SQL-import only.

Verification:
- Ran `node --check app.js`.
  - Response: success, no syntax errors.
- Ran `rg` verification for SQL stock import flow and old manual receiving tokens.
  - Response confirmed:
    - `APP_BUILD = "20260604-b2c-sql-stock-import"`;
    - `SQL_STOCK_RECEIPT_SOURCE`;
    - `sync-sql-stock-receipts`;
    - `syncStockReceiptsFromSql()`;
    - no `receiveStock` handler in app code.
- Ran DOM smoke-test with `currentView: "stock"`.
  - Response: `stock-render-ok appHtml=3547 navHtml=674`.
- Ran final manual stock receiving token check:
  - Command searched `app.js`, `index.html`, and `README.md` for `receive-stock`, `receiveStock`, and `Оприбуткувати`.
  - Response: `no-manual-stock-receiving`.

## 17:34 - Inventory and scanner request

User request:
- `в залишки додай інвентаризацію із роздруком і введенням фактичних залишків і підрахунком різниці та штрихкодовим\Qr скануванням . при створенні продажі теж пошук за штрххкодом і Qr`

Interpretation:
- Keep stock receiving SQL-import only.
- Add a separate inventory count workflow inside B2C stock/залишки:
  - print inventory sheet;
  - enter actual counted quantities;
  - calculate difference between accounting stock and actual stock;
  - barcode/QR scanner input for finding/counting items.
- Enhance sales checkout search/scanning:
  - search by barcode and QR;
  - scanner input should add exact barcode/QR matches to the cart.

Implementation:
- Updated build metadata:
  - `APP_VERSION = "2026.06.04.6"`.
  - `APP_BUILD = "20260604-b2c-inventory-scan"`.
  - `STORAGE_KEY = "retail-crm-b2c-v5"`.
- Added QR payloads to SQL product snapshot:
  - format: `B2C|SKU=...|BARCODE=...`.
- Added scan/search helpers:
  - `normalizeScanText()`;
  - `scanTokens()`;
  - `productScanTargets()`;
  - `productMatchesQuery()`;
  - `findProductByScan()`.
- Updated checkout:
  - search label now includes SKU/barcode/QR;
  - product filtering now uses `productMatchesQuery()`;
  - added `data-checkout-scan` scanner input;
  - Enter/change on scanner adds exact barcode/QR match to the cart.
- Added inventory state:
  - `inventory`;
  - `inventoryDocs`;
  - product-level expected quantity and actual quantity.
- Added inventory UI in `Залишки`:
  - metrics for total/count/overage/shortage;
  - barcode/QR scanner input;
  - actual quantity inputs;
  - accounting stock, actual stock, and difference table;
  - printable inventory sheet;
  - recent inventory documents table.
- Added inventory actions:
  - `scanInventoryProduct()`;
  - `setInventoryActual()`;
  - `resetInventoryDraft()`;
  - `printInventorySheet()`;
  - `postInventory()`.
- Added print CSS:
  - hides navigation/buttons;
  - prints the inventory sheet cleanly.
- Updated README:
  - checkout search includes QR;
  - stock includes inventory with print, actual stock, difference, barcode/QR scanning.

Verification:
- Ran `node --check app.js`.
  - Response: success, no syntax errors.
- Ran `rg` verification for build, QR, checkout scanner, inventory scanner/actions, and old stock receiving tokens.
  - Response confirmed:
    - `APP_BUILD = "20260604-b2c-inventory-scan"`;
    - product `qr` fields;
    - `productMatchesQuery()`;
    - `findProductByScan()`;
    - `data-checkout-scan`;
    - `data-inventory-scan`;
    - `data-inventory-actual`;
    - `post-inventory`;
    - `printInventorySheet()`.
- Ran checkout scan behavior test with QR payload.
  - Response: `checkout-scan-ok qty=1 appHtml=4364`.
- Ran inventory behavior test with QR and barcode scans, actual quantity entry, difference calculation, print call, and posting.
  - Response: `inventory-ok docs=1 p100=5 p102=2`.

## 17:52 - Inventory prices, search, and re-sort request

User request:
- `у інвентаризацію підтягни розрібні ціни , можливість пошуку по назві , друк Інвентаризаційний лист , і можливість проводити пересорти тобто один товар заміняти по кількості на інший із підсумком по грошах плюс на мінус`

Interpretation:
- Inventory must show retail prices from the SQL product catalog.
- Inventory must support search by product name, while keeping SKU/barcode/QR search useful.
- Print action should clearly print the `Інвентаризаційний лист`.
- Inventory must support re-sort/peresort:
  - one product quantity goes into minus;
  - another product quantity goes into plus;
  - money summary must show plus, minus, and net difference by retail prices.

Implementation:
- Updated build metadata:
  - `APP_VERSION = "2026.06.04.7"`.
  - `APP_BUILD = "20260604-b2c-inventory-resort"`.
  - `STORAGE_KEY = "retail-crm-b2c-v6"`.
- Extended inventory state:
  - added `inventory.search`;
  - added `inventory.resorts`;
  - added amount fields in posted inventory documents.
- Inventory rows now calculate retail-price values:
  - retail price;
  - accounting amount;
  - actual amount;
  - quantity difference;
  - money difference.
- Inventory UI now includes:
  - search field by product name/SKU/barcode/QR;
  - retail price column;
  - `+/- грн` column;
  - money summary cards for plus and minus by retail value;
  - print button labeled `Друк Інвентаризаційний лист`.
- Printable `Інвентаризаційний лист` now includes:
  - retail price;
  - quantity difference;
  - money difference;
  - plus/minus/net money total;
  - re-sort section.
- Added re-sort/peresort workflow:
  - `createInventoryResort()`;
  - `removeInventoryResort()`;
  - `inventoryResortTotals()`;
  - `renderInventoryResorts()`.
- Re-sort behavior:
  - selected `Мінус товар` reduces actual inventory quantity;
  - selected `Плюс товар` increases actual inventory quantity;
  - row stores retail prices, minus amount, plus amount, and net amount.
- Posted inventory document now stores:
  - price and amount columns per inventory line;
  - total money difference;
  - re-sort rows and their net money result.
- Updated `README.md` with retail prices, search, print inventory sheet, and re-sort plus/minus money summary.

Verification:
- Ran `node --check app.js`.
  - Response: success, no syntax errors.
- Ran `rg` verification for build, inventory search, re-sort functions, retail-price labels, and print label.
  - Response confirmed:
    - `APP_BUILD = "20260604-b2c-inventory-resort"`;
    - `STORAGE_KEY = "retail-crm-b2c-v6"`;
    - `data-inventory-search`;
    - `createInventoryResort()`;
    - `removeInventoryResort()`;
    - `inventoryResortTotals()`;
    - `renderInventoryResorts()`;
    - `Друк Інвентаризаційний лист`.
- First behavior test failed because it expected `data-remove-resort` in an empty draft.
  - Response: `Error: inventory price/search/resort UI missing`.
- Diagnostic render check showed:
  - `hasSearch: true`;
  - `hasCreateResort: true`;
  - `hasRemoveResort: false`;
  - reason: remove button exists only after a re-sort row is created.
- Re-ran behavior test correctly:
  - created re-sort `-OPT-RPOINT +EYE-PRO`, qty `2`;
  - verified minus amount `10800`;
  - verified plus amount `1960`;
  - verified net amount `-8840`;
  - verified actual quantity changes;
  - canceled re-sort and verified actual quantities returned;
  - recreated re-sort;
  - called print;
  - posted inventory document.
  - Response: `inventory-resort-ok doc=INV-0001 amount=-8840 resorts=1`.

## 18:08 - Inventory stock posting and employees/roles request

User request:
- `тепер - проведення інвентаризації змінює залишки на складі . і додай блок працівники - ролі Директор адміністратор продавець касир де мають бути всі поля блоки дії і тд`

Interpretation:
- Inventory posting must explicitly update store/warehouse stock balances, not only create an inventory document.
- Add a separate B2C employees block.
- Employee roles required:
  - Director;
  - Administrator;
  - Seller;
  - Cashier.
- Employees block must include:
  - employee fields;
  - role blocks/access;
  - role actions;
  - operational actions.

Implementation:
- Updated `app.js`:
  - `APP_VERSION = "2026.06.04.8"`;
  - `APP_BUILD = "20260604-b2c-employees-roles"`;
  - `STORAGE_KEY = "retail-crm-b2c-v7"`;
  - added `EMPLOYEE_STATUSES`, `ROLE_BLOCKS`, `ROLE_ACTIONS`, `EMPLOYEE_ROLES`;
  - added seeded employees for Director, Administrator, Seller, Cashier;
  - added `selectedCashierId`;
  - added navigation item `employees`;
  - added `normalizeEmployee()`, `employeeById()`, `activeEmployees()`, `roleLabel()`, `employeeStatusLabel()`, `roleHas()`;
  - inventory document now stores `appliedToStock` and `stockAdjustments`;
  - `postInventory()` now writes actual counted quantities back into `state.stock` through `stockRow(productId).qty = row.actualQty`;
  - inventory button label changed to `Провести і оновити склад`;
  - inventory documents table now shows stock status `оновлено` or `чернетка`;
  - added `renderEmployees()` with employee card table, create employee form, role/block matrix, role/action matrix;
  - added `createEmployee()`, `toggleEmployeeStatus()`, `selectCashier()`;
  - open cash shift now uses selected employee and writes `cashierId`, `cashier`, `cashierRole`.
- Updated `index.html` cache/version strings to `20260604-b2c-employees-roles`.
- Updated `README.md`:
  - inventory posting updates stock balances;
  - employees block with roles and access/action matrices.

Verification:
- Command:
  - `node --check app.js`
  - Response: exit code `0`, no syntax errors.
- Command:
  - `rg -n "20260604-b2c-employees-roles|renderEmployees|createEmployee|toggleEmployeeStatus|selectCashier|appliedToStock|stockAdjustments|Провести і оновити склад|Працівники|EMPLOYEE_ROLES|ROLE_BLOCKS|ROLE_ACTIONS" app.js index.html README.md logs\B2C_WORK_LOG_2026-06-04.md`
  - Response: found expected version, employees, inventory posting, role matrix, and stock adjustment identifiers.
- Behavior test attempt 1:
  - Tool: `node_repl`;
  - Response: `node_repl kernel exited unexpectedly`, diagnostics included `windows sandbox failed: spawn setup refresh`.
- Behavior test attempt 2:
  - Command: Node `-e` with the behavior-check script;
  - Response: `SyntaxError: Unexpected end of input`;
  - Cause: PowerShell argument passing stripped quotes from the large JavaScript snippet.
- Behavior test attempt 3:
  - Command: Node through stdin;
  - Response: `Error: required roles missing`;
  - Cause: Ukrainian text literals in stdin were recoded by the PowerShell pipeline, so the test was changed to verify ASCII role identifiers and app functions instead of console-encoded display text.
- Behavior test final:
  - Command: Node through stdin with ASCII role checks and VM sandbox;
  - Checked:
    - employees view renders `data-action="create-employee"`;
    - roles `director`, `admin`, `seller`, `cashier` exist;
    - Director/Admin/Seller access matrix values are correct;
    - cashier select action exists;
    - employee creation works;
    - active cashier auto-select works;
    - toggling an active selected cashier to inactive removes that employee from `selectedCashierId`;
    - selecting seeded cashier `e-004` works;
    - opening cash shift stores employee fields;
    - inventory posting changes stock quantity for `p-100`;
    - inventory doc stores `appliedToStock = true`;
    - inventory doc stores `stockAdjustments` with before/after/diff.
  - Response: `behavior-ok employees=5 selectedCashier=e-004 stockP100=5`.
- Browser verification attempt:
  - Tool: Browser plugin via `node_repl`;
  - Target: `file:///D:/Codex/CRM/retail-crm/index.html`;
  - Response: `node_repl kernel exited unexpectedly`, diagnostics included `windows sandbox failed: spawn setup refresh`.
  - Result: in-app browser could not be used in this environment; Node behavior and syntax checks passed.

## 20:53 - Combine new check, checks, cash and POS into one block

User request:
- `новий чек , чеки , Каса та POS -обєднуємо в один блок`

Interpretation:
- In the B2C retail prototype, `Новий чек`, `Чеки`, and `Каса/POS` must become one navigation/work block.
- The separate `Повернення` block remains separate because it was not included in the merge request.
- Existing internal references to old views `checkout`, `receipts`, and `cash` must route to the new combined block.

Implementation:
- Updated `app.js`:
  - `APP_VERSION = "2026.06.04.9"`;
  - `APP_BUILD = "20260604-b2c-pos-combined"`;
  - `STORAGE_KEY = "retail-crm-b2c-v8"`;
  - replaced role blocks `checkout`, `receipts`, `cash` with one role block `pos`;
  - updated Director/Admin/Seller/Cashier block access to use `pos`;
  - changed navigation from three items `Новий чек`, `Чеки`, `Каса/POS` to one item `POS / Чеки / Каса`;
  - added `VIEW_ALIASES = { checkout: "pos", receipts: "pos", cash: "pos" }`;
  - added `normalizeView()` and used it during state normalization and navigation clicks;
  - added `renderPos()` with one screen containing POS summary metrics, new-check form, checks journal/print preview, and cash-shift panel/history;
  - kept legacy `checkout`, `receipts`, and `cash` view ids mapped to `renderPos()` for compatibility;
  - changed post-sale transition to `state.currentView = "pos"`;
  - changed customer selection transition to `state.currentView = "pos"`;
  - changed cashier selection transition to `state.currentView = "pos"`.
- Updated `index.html` cache/version strings to `20260604-b2c-pos-combined`.
- Updated `README.md` to describe the unified POS block.

Commands and responses:
- Command: `git status --short --branch`
  - Response: `## main...origin/main`
- Command: `rg -n "APP_VERSION|APP_BUILD|STORAGE_KEY|navItems|ROLE_BLOCKS|EMPLOYEE_ROLES|function renderCheckout|function renderReceipts|function renderCash|function render\(|checkout: renderCheckout|receipts: renderReceipts|cash: renderCash|currentView" app.js index.html README.md`
  - Response: found old version/build, separate role blocks and view renderers before edits.
- Command: `node --check app.js`
  - Response: exit code `0`, no syntax errors.
- Command: `rg -n "20260604-b2c-pos-combined|retail-crm-b2c-v8|renderPos|VIEW_ALIASES|POS / Чеки / Каса|state\.currentView = \"pos\"|pos: renderPos|checkout: renderPos|receipts: renderPos|cash: renderPos" app.js index.html README.md`
  - Response: failed because PowerShell treated `|` inside the command string as a pipeline separator.
- Follow-up fixed-string searches:
  - `rg -n -F "20260604-b2c-pos-combined" app.js index.html README.md`
    - Response: found build in `app.js` and cache strings in `index.html`.
  - `rg -n -F "retail-crm-b2c-v8" app.js index.html README.md`
    - Response: found storage key in `app.js`.
  - `rg -n -F "renderPos" app.js`
    - Response: found `renderPos()` and legacy view mappings to `renderPos`.
  - `rg -n -F "VIEW_ALIASES" app.js`
    - Response: found old-view aliases and `normalizeView()` usage.
  - `rg -n -F "POS / Чеки / Каса" app.js index.html README.md`
    - Response: found new navigation item in `app.js`.
  - `rg -n "currentView.*pos" app.js`
    - Response: found transitions at lines `1544`, `1588`, `1890`.
- Behavior test:
  - Command: Node through stdin with VM sandbox.
  - Checked:
    - version/build/storage key;
    - `checkout`, `receipts`, `cash` normalize to `pos`;
    - nav contains `pos` and no longer contains old separate view ids;
    - role blocks contain `pos` and no longer contain old separate role blocks;
    - Seller and Cashier have access to `pos`;
    - combined POS HTML contains `B2C.1`, `B2C.2`, `B2C.7`;
    - combined POS HTML contains create receipt, print receipt and close shift actions;
    - creating a receipt keeps `currentView = "pos"`;
    - selecting a customer routes to `pos`;
    - selecting a cashier routes to `pos`.
  - Response: `pos-combined-ok currentView=pos receipts=2 selectedCashier=e-004`.
- Command: `Get-Date -Format "HH:mm"`
  - Response: `20:53`.

## 20:59 - LAN viewer timeout on old IP address

User request / screenshot:
- Browser opened `http://192.168.0.161:8790/index.html`.
- Chrome response:
  - `Немає зв'язку із сайтом`;
  - `Хост 192.168.0.161 довго не відповідає`;
  - `ERR_CONNECTION_TIMED_OUT`.

Diagnosis:
- The B2C LAN server is running on port `8790`.
- The current Wi-Fi LAN IP of this machine is `192.168.4.14`, not `192.168.0.161`.
- The address in the browser is stale or belongs to another network/subnet.

Commands and responses:
- Command: `Get-Date -Format "HH:mm"`
  - Response: `20:59`.
- Command: `git status --short --branch`
  - Response: `## main...origin/main`.
- Command: `Get-Content serve-lan.ps1`
  - Response: confirmed `serve-lan.ps1` binds to `[System.Net.IPAddress]::Any` on port `8790`, serves static files from the B2C root, and prints LAN URLs.
- Command: `netstat -ano | findstr ":8790"`
  - Response:
    - `0.0.0.0:8790 LISTENING 18396`;
    - `100.64.4.110:8790 LISTENING 8492`;
    - `[fd7a:115c:a1e0::7d34:46e]:8790 LISTENING 8492`.
- Command: `ipconfig`
  - Response:
    - Wi-Fi adapter `Беспроводная сеть 3` has IPv4 `192.168.4.14`;
    - Tailscale has IPv4 `100.64.4.110`;
    - old address `192.168.0.161` is not present.
- Command: `Invoke-WebRequest http://127.0.0.1:8790/index.html`
  - Response: `local 200 20260604-b2c-pos-combined`.
- Command: `Invoke-WebRequest http://192.168.4.14:8790/index.html`
  - Response: `wifi 200 20260604-b2c-pos-combined`.
- Command: `Get-Process -Id 18396,8492 | Select-Object Id,ProcessName,Path`
  - Response:
    - `18396 powershell`;
    - `8492 tailscaled`.

Correct user-facing URL:
- `http://192.168.4.14:8790/index.html`

## 21:07 - Rename new check to sale

User request:
- `новий чек має називатись - Продаж`

Interpretation:
- Visible UI label `Новий чек` must become `Продаж`.
- Technical code identifiers such as `receipt`, `checkout`, and `create-receipt` stay unchanged to avoid breaking already verified receipt journal and POS actions.

Implementation:
- Updated `app.js`:
  - `APP_VERSION = "2026.06.04.10"`;
  - `APP_BUILD = "20260604-b2c-sale-label"`;
  - kept `STORAGE_KEY = "retail-crm-b2c-v8"` because this is a label/cache update, not a data-shape change;
  - page title from `Новий чек магазину` to `Продаж`;
  - section header from `B2C.1 Новий чек` to `B2C.1 Продаж`;
  - comment placeholder from `примітка до чека` to `примітка до продажу`;
  - submit button from `Провести чек` to `Провести продаж`;
  - audit event from `Проведено чек ...` to `Проведено продаж ...`;
  - customer selection audit from `для нового чека` to `для продажу`.
- Updated `index.html` cache/version strings to `20260604-b2c-sale-label`.
- Updated `README.md` POS description from `новий чек` to `продаж`.

Commands and responses:
- Command: `Get-Date -Format "HH:mm"`
  - Response: `21:07`.
- Command: `git status --short --branch`
  - Response: `## main...origin/main [ahead 1]`.
- Command: `rg -n "Новий чек|новий чек|Новий|B2C\.1|create-receipt|receipt" app.js index.html README.md logs\B2C_WORK_LOG_2026-06-04.md`
  - Response:
    - active UI hits included `app.js:680 setTitle("Новий чек магазину")`, `app.js:714 <h2>B2C.1 Новий чек</h2>`, and `README.md:19`;
    - historical log entries also contained older `Новий чек` references.
- Command: `node --check app.js`
  - Response: exit code `0`, no syntax errors.
- Command: `rg -n "Новий чек|новий чек" app.js index.html README.md`
  - Response: exit code `1`, no matches in active UI/docs files.
- Command: `rg -n "2026\.06\.04\.10|20260604-b2c-sale-label|B2C\.1 Продаж|Провести продаж|примітка до продажу|вибрано для продажу|Проведено продаж" app.js index.html README.md`
  - Response:
    - found cache strings in `index.html`;
    - found app version/build in `app.js`;
    - found `B2C.1 Продаж`;
    - found `Провести продаж`;
    - found `примітка до продажу`;
    - found updated audit texts.
- Behavior test attempt 1:
  - Command: Node through stdin with VM sandbox.
  - Response: `SyntaxError: missing ) after argument list`.
  - Cause: test string for `data-action="create-receipt"` had unescaped double quotes.
- Behavior test final:
  - Command: Node through stdin with VM sandbox and Unicode-escaped Ukrainian labels.
  - Checked:
    - version/build;
    - rendered POS view contains `B2C.1 Продаж`;
    - rendered POS view still contains `data-action="create-receipt"`;
    - rendered POS view no longer contains `Новий чек`.
  - Response: `sale-label-ok build=20260604-b2c-sale-label`.

## 21:17 - Rename combined POS/checks/cash block to sale

User request:
- `переіменуй це POS / чеки / каса в продаж`

Interpretation:
- The combined block label `POS / Чеки / Каса` / `POS / чеки / каса` must be renamed to `Продаж`.
- The internal view id `pos` remains unchanged for compatibility with existing aliases and routes.
- `STORAGE_KEY` remains unchanged because this is a label/cache update, not a data-shape change.

Implementation:
- Updated `app.js`:
  - `APP_VERSION = "2026.06.04.11"`;
  - `APP_BUILD = "20260604-b2c-sale-block-label"`;
  - role block label `{ id: "pos" }` changed from `POS / чеки / каса` to `Продаж`;
  - navigation item `["pos", ...]` changed from `POS / Чеки / Каса` to `Продаж`;
  - `renderPos()` page title changed from `POS / чеки / каса` to `Продаж`.
- Updated `index.html` cache/version strings to `20260604-b2c-sale-block-label`.
- Updated `README.md` from `єдиний POS-блок` to `єдиний блок Продаж`.

Commands and responses:
- Command: `Get-Date -Format "HH:mm"`
  - Response: `21:17`.
- Command: `git status --short --branch`
  - Response: `## main...origin/main [ahead 2]`.
- Command: `rg -n "POS / Чеки / Каса|POS / чеки / каса|POS-блок|renderPos|APP_VERSION|APP_BUILD|b2c-sale-label|Продаж" app.js index.html README.md logs\B2C_WORK_LOG_2026-06-04.md`
  - Response:
    - found active `POS-блок` in `README.md:19`;
    - found active cache strings `20260604-b2c-sale-label` in `index.html`;
    - found `APP_VERSION = "2026.06.04.10"` and `APP_BUILD = "20260604-b2c-sale-label"`;
    - found `ROLE_BLOCKS` pos label `POS / чеки / каса`;
    - found nav item `POS / Чеки / Каса`;
    - found `renderPos()` title `POS / чеки / каса`;
    - found historical log entries with older POS names.
- Command: `node --check app.js`
  - Response: exit code `0`, no syntax errors.
- Command: `rg -n "POS / Чеки / Каса|POS / чеки / каса|POS-блок" app.js index.html README.md`
  - Response: exit code `1`, no matches in active UI/docs files.
- Command: `rg -n "2026\.06\.04\.11|20260604-b2c-sale-block-label|\[\"pos\", \"Продаж\"\]|id: \"pos\", label: \"Продаж\"|setTitle\(\"Продаж\"\)|єдиний блок `Продаж`" app.js index.html README.md`
  - Response: PowerShell parser error because the backtick in the README search string was treated as an unterminated quote.
- Follow-up fixed-string searches:
  - `rg -n -F "2026.06.04.11" app.js index.html README.md`
    - Response: found version in `app.js` and visible version in `index.html`.
  - `rg -n -F "20260604-b2c-sale-block-label" app.js index.html README.md`
    - Response: found build in `app.js` and cache strings in `index.html`.
  - `rg -n -F "єдиний блок `Продаж`" README.md`
    - Response: found README line `19`.
- Command: `Get-Content app.js | Select-Object -First 30`
  - Response: showed app version/build and role block label, but PowerShell rendered Ukrainian as mojibake in the console output.
- Command: `Get-Content app.js | Select-Object -Skip 218 -First 12`
  - Response: showed nav item label, but PowerShell rendered Ukrainian as mojibake in the console output.
- Command: `Get-Content app.js | Select-Object -Skip 680 -First 10`
  - Response: showed `renderPos()` title, but PowerShell rendered Ukrainian as mojibake in the console output.
- Behavior test:
  - Command: Node through stdin with VM sandbox and Unicode-escaped labels.
  - Checked:
    - `APP_VERSION === "2026.06.04.11"`;
    - `APP_BUILD === "20260604-b2c-sale-block-label"`;
    - `ROLE_BLOCKS.find(id === "pos").label === "Продаж"`;
    - `navItems.find(id === "pos")[1] === "Продаж"`;
    - rendered page title is `Продаж`;
    - rendered POS view no longer contains old `POS / Чеки / Каса` or `POS / чеки / каса`.
  - Response: `sale-block-label-ok nav=?????? title=??????` because the console displayed Cyrillic as question marks, while the Unicode-escaped assertions passed.

## 21:27 - Move products, customers and stock under directories submenu

User request:
- `товари клієнти залишки в одне підменю від довідники`

Interpretation:
- In B2C navigation, `Товари`, `Клієнти`, and `Залишки` must no longer be top-level menu items.
- They must appear as child items under one submenu named `Довідники`.
- The screens and role permissions for `catalog`, `customers`, and `stock` remain separate to preserve their existing logic and access granularity.
- `STORAGE_KEY` remains unchanged because this is a navigation/cache update, not a data-shape change.

Implementation:
- Updated `app.js`:
  - `APP_VERSION = "2026.06.04.12"`;
  - `APP_BUILD = "20260604-b2c-directories-submenu"`;
  - changed `navItems` from a flat list to a mixed list with group `{ id: "directories", label: "Довідники", children: [...] }`;
  - moved child nav items into `Довідники`:
    - `catalog` / `Товари`;
    - `customers` / `Клієнти`;
    - `stock` / `Залишки`;
  - added `flatNavItems()` so `normalizeView()` still recognizes child views;
  - updated `renderNav()` to render both top-level buttons and grouped submenu buttons.
- Updated `styles.css`:
  - added `.nav-group`;
  - added `.nav-group-label`;
  - added `.nav-group.open .nav-group-label`;
  - added `.nav-submenu`;
  - added `.nav-submenu button`.
- Updated `index.html` cache/version strings to `20260604-b2c-directories-submenu`.
- Updated `README.md`:
  - replaced separate bullets for goods/customers/stock with one `Довідники` submenu bullet.

Commands and responses:
- Command: `Get-Date -Format "HH:mm"`
  - Response: `21:27`.
- Command: `git status --short --branch`
  - Response: `## main...origin/main [ahead 3]`.
- Command: `rg -n "APP_VERSION|APP_BUILD|STORAGE_KEY|navItems|renderNav|data-view|catalog|customers|stock|Товари|Клієнти|Залишки|Довідники" app.js index.html README.md styles.css logs\B2C_WORK_LOG_2026-06-04.md`
  - Response:
    - found current version/build before edit;
    - found flat nav items `Товари`, `Клієнти`, `Залишки`;
    - found current `renderNav()` flat-button implementation;
    - found existing `.nav` styles.
- Command: `Get-Content styles.css | Select-Object -First 180`
  - Response: displayed existing sidebar/nav styles; no submenu styles existed before edit.
- Command: `node --check app.js`
  - Response: exit code `0`, no syntax errors.
- Command: `rg -n "2026\.06\.04\.12|20260604-b2c-directories-submenu|Довідники|flatNavItems|nav-group|nav-submenu|directories" app.js index.html README.md styles.css`
  - Response:
    - found README submenu line;
    - found `.nav-group` and `.nav-submenu` styles;
    - found new cache strings in `index.html`;
    - found app version/build;
    - found `directories` group;
    - found `flatNavItems()`;
    - found grouped nav HTML in `renderNav()`.
- Command: `rg -n "\[\"catalog\", \"Товари\"\]|\[\"customers\", \"Клієнти\"\]|\[\"stock\", \"Залишки\"\]" app.js`
  - Response: regex parse error due unescaped `[` in the alternation pattern.
- Follow-up fixed-string searches:
  - `rg -n -F '[\"catalog\", \"Товари\"]' app.js`
  - `rg -n -F '[\"customers\", \"Клієнти\"]' app.js`
  - `rg -n -F '[\"stock\", \"Залишки\"]' app.js`
  - Response: exit code `1`; the command searched for escaped quotes literally. A VM structure test was used instead.
- Behavior test:
  - Command: Node through stdin with VM sandbox.
  - Checked:
    - `APP_VERSION === "2026.06.04.12"`;
    - `APP_BUILD === "20260604-b2c-directories-submenu"`;
    - top-level `navItems` no longer contains `catalog`, `customers`, `stock`;
    - `directories` group exists;
    - `directories.children` are exactly `catalog,customers,stock`;
    - `flatNavItems()` still includes child views;
    - `normalizeView("stock") === "stock"`;
    - when `state.currentView = "customers"`, rendered nav contains `.nav-group open`;
    - rendered submenu contains buttons for `data-view="catalog"`, `data-view="customers"`, and `data-view="stock"`.
  - Response: `directories-submenu-ok children=catalog,customers,stock active=customers`.
- LAN cache check:
  - Command: `Invoke-WebRequest http://127.0.0.1:8790/index.html`
    - Response: `local 200 20260604-b2c-directories-submenu`.
  - Command: `Invoke-WebRequest http://192.168.4.14:8790/index.html`
    - Response: `wifi 200 20260604-b2c-directories-submenu`.
